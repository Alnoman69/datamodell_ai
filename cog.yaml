build:
  python_version: "3.10"
  system_packages: []
  python_packages:
    - torch
    - torchvision
    - diffusers
    - transformers
    - pytorch-lightning
    - accelerate
    - tqdm
    - Pillow

train:
  script: train.py
  args:
    epochs:
      type: int
      default: 3
    batch_size:
      type: int
      default: 1

predict: predict.py:Predictor